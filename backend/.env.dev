DB_USERNAME=db_user
DB_PASSWORD=secret
DB_HOST=localhost
DB_NAME=todo

BACKEND_CORS_ORIGINS="http://localhost,http://localhost:5173,https://localhost,https://localhost:5173"

SECRET_KEY=secret # generate with 'openssl rand -hex 32'
ACCESS_TOKEN_EXPIRE_MINUTES=5 # default to 60